import axios from 'axios';

export const userRegister = (userData) => {
  const userResponseData = userData;
  return userResponseData;
};

export const adminRegister = (adminData) => {
  const adminResponseData = adminData;
  return adminResponseData;
};

export const login = (loginValue) => {
  const loginData = loginValue;
  return loginData;
};