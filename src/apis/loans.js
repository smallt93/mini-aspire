import axios from 'axios';

export const loanSubmit = (loanValue) => {
  const loanResponseData = loanValue;
  return loanResponseData;
};

export const loanRepay = (loanId) => {
  const repayResponseData = loanId;
  return repayResponseData;
}
